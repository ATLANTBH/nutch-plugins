<?xml version="1.0" encoding="UTF-8"?>
	
<!-- List of properties -->
<!-- (R)filteringType: Defines the type of filtering. If it's going to be a whitelist or a blacklist filtering (WHITELIST, BLACKLIST) -->
	
<omitIndexingFilterConfiguration filteringType="WHITELIST">

	<!-- List of properties -->
	<!-- (R)target: Defines the target against which the regex should be matched (URL, META_FIELD_PRESENT) -->
	<!-- (O)regex: If target set to URL defines the regex that should be used for matching -->
	
	<!-- (O)name: If target set to META_FIELD_PRESENT defines the name of the meta field that should be contained -->
	<omitIndexingFilterConfigurationEntry target="URL" regex="^http://([a-z0-9]*\.)*test.com/" />
	
</omitIndexingFilterConfiguration>